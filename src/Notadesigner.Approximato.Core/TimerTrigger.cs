namespace Notadesigner.Approximato.Core
{
    public enum TimerTrigger
    {
        Abandon,
        Continue,
        Focus,
        Interrupt,
        Reset,
        Resume,
        Timeout
    }
}